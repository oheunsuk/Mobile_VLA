# Inference

This directory is reserved for checkpoint loading and runtime inference modules.
The current runnable offline path uses `models/StubOmniVlaGnmPolicy` until real
OmniVLA/GNM checkpoints are available.
